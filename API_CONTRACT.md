# API Contract

This file aliases the current full local route contract.

See:

- `FULL_APP_ROUTE_CONTRACT.md`

That document is the maintained current contract between:

- backend (`Socity_backend`)
- Flutter app (`OptiZenqor_social`)
- dashboard (`OptiZenqor_social_dashboard`)
